name: Test macOS Setup

# DESATIVADO: Este workflow foi desativado temporariamente.
# A versão anterior testava em ubuntu-latest e macos-latest,
# mas os testes de playbook macOS em Linux produzem falsos positivos.
# Para reativar, descomente as linhas abaixo.
#
# on:
#   push:
#     branches: [ master, main ]
#   pull_request:
#     branches: [ master, main ]
#   workflow_dispatch:

on: workflow_dispatch

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  placeholder:
    name: CI Desativada
    runs-on: ubuntu-latest
    steps:
    - name: Explicacao
      run: |
        echo "CI desativada. Veja comentarios no arquivo para reativar."
        echo "Para CI real de macOS, analisar Docker-OSX:"
        echo "https://github.com/sickcodes/Docker-OSX"
